// Only public connection settings belong here. Never add mailbox addresses or credentials.
window.WEAR_CONTACT = Object.freeze({
  enabled: true,
  endpoint: "https://us-central1-wearglasgow.cloudfunctions.net/submitEnquiry"
});
